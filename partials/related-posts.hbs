
{{#get "posts" limit="5" filter="tags:[{{post.tags}}]+tags:nyhed+id:-{{post.id}}" include="tags" as |related|}}
                {{#foreach related}}
                <div class="related-posts__post">
                    {{> "image-box"}}
                </div>
                {{/foreach}}
                {{else}}
                <p>No related posts</p>
                {{/get}}